# PATENTEXT API

## Conf

```
cd ${PROJECT_ROOT}
touch .env
cat .env.dist >> .env
```

Edit parameters inside <strong style="color:gray">.env</strong> as needed

### Setup

```
npm install
npm start
```